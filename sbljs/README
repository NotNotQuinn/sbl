
	sbljs: Minify & bundle JavaScript meant to run on supibot.
	booleans:
			--watch -w   (default: false)
					Compile on file changes.

			--minify -m   (default: false)
					Minify the output.

			--skip-type-check   (default: false)
					Do not run the type checker (tsc).

			--help -h   (default: false)
					Show this help text.
